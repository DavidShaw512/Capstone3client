import React from 'react';

import './addNew.css';

const AddNew = (props) => {
    return (
        <div className="addNew">
            <span className="fas fa-plus"></span>
        </div>
    )
};

export default AddNew;